# Never commit repository-specific config (the per-spec layer).
# It lives centrally and is symlinked into spec repos as .respec-skill.md.
.respec-skill.md
*.respec-skill.md
respec-skill-configs/
*.skill
